初始化一个8个元素的随机整型数组，每个元素都是2位10进制数，对它进行排序，要求奇数放在数组前半部分，偶数放在数组后半部分，且奇数、偶数都要按从小到大顺序排列。
可以尝试不同方法：
1、先分成奇数、偶数前后两部分，再分别对这两部分用冒泡排序。
2、比如从数组开头只遍历一次就排好顺序。

t2.c是常规排序，先交换奇偶数位置，再分别对奇数部分、偶数部分排序。
t1.c是特殊排序，一次遍历就排好顺序。

